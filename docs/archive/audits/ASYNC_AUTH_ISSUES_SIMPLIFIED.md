# Async & Auth Issues - Simplified Visual Guide

## What's Happening on Your Screen

```
┌─────────────────────────────────────────────────────────────────────┐
│                     MARKET OVERVIEW PAGE                            │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│   ┌─────────────────────┐    ┌─────────────────────┐               │
│   │ Market Compression  │    │ Absolute PSF        │               │
│   │                     │    │                     │               │
│   │    ⟳ Updating...    │    │    ⟳ Updating...    │               │
│   │                     │    │                     │               │
│   │    0 periods        │    │    0 periods        │               │
│   └─────────────────────┘    └─────────────────────┘               │
│                                                                     │
│   ┌───────────────────────────────────────────────┐                │
│   │ Market Value Oscillator                       │                │
│   │                                               │                │
│   │    ⟳ Updating...         DIVERGENCE: NaNo    │                │
│   │                                               │                │
│   └───────────────────────────────────────────────┘                │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘

         ↑                           ↑                    ↑
         │                           │                    │
    ISSUE #1                    ISSUE #1              ISSUE #2
    Stuck loading               Stuck loading         NaN display bug
```

---

## The App Boot Process (Simplified)

Think of it like a relay race where each runner must finish before the next can start:

```
┌──────────────────────────────────────────────────────────────────────┐
│                         APP STARTUP SEQUENCE                         │
└──────────────────────────────────────────────────────────────────────┘

   🏃 Runner 1          🏃 Runner 2           🏃 Runner 3        🏁 FINISH
   ──────────>          ──────────>           ──────────>

┌──────────┐        ┌───────────────┐      ┌─────────────┐     ┌─────────┐
│ Firebase │   →    │ Get User's    │  →   │ Load Filter │  →  │ Charts  │
│ Login    │        │ Subscription  │      │ Settings    │     │ Can     │
│          │        │               │      │             │     │ Load!   │
└──────────┘        └───────────────┘      └─────────────┘     └─────────┘
     │                     │                      │                  │
     ▼                     ▼                      ▼                  ▼
  "Who is            "Free or              "What filters       "OK, fetch
   this user?"        Premium?"             were saved?"        the data!"



❌ THE PROBLEM: If ANY runner gets stuck, the race never finishes!

┌──────────┐        ┌───────────────┐
│ Firebase │   →    │ STUCK HERE!   │  →   ❌ Charts never load
│ Login ✓  │        │ Token refresh │
│          │        │ keeps failing │
└──────────┘        └───────────────┘
```

---

## Issue #1: The "Stuck Updating" Problem

### What Should Happen (Normal Flow)

```
USER CHANGES FILTER
        │
        ▼
┌───────────────┐     ┌───────────────┐     ┌───────────────┐
│ Status:       │     │ Status:       │     │ Status:       │
│ "loading"     │ ──▶ │ "refreshing"  │ ──▶ │ "success"     │
│               │     │ (shows blur)  │     │ (shows chart) │
│ Show skeleton │     │ + spinner     │     │               │
└───────────────┘     └───────────────┘     └───────────────┘
      1 sec               2 sec                  DONE ✓
```

### What's Actually Happening (Bug)

```
USER CHANGES FILTER
        │
        ▼
┌───────────────┐     ┌───────────────┐
│ Status:       │     │ Status:       │     ┌─────────────────┐
│ "loading"     │ ──▶ │ "refreshing"  │ ──▶ │ STUCK FOREVER!  │
│               │     │               │     │                 │
│ Show skeleton │     │ Request gets  │     │ Never changes   │
└───────────────┘     │ ABORTED but   │     │ to "success"    │
                      │ status doesn't│     │                 │
                      │ update!       │     │ ⟳ Updating...   │
                      └───────────────┘     └─────────────────┘
```

### Why Does This Happen?

```
┌─────────────────────────────────────────────────────────────────────┐
│                     THE ABORT BUG EXPLAINED                         │
└─────────────────────────────────────────────────────────────────────┘

STEP 1: Request starts
┌──────────────────┐
│ loading = TRUE   │  ← "I'm fetching data!"
│ data = [old]     │
└──────────────────┘

STEP 2: Something causes abort (filter change, network hiccup)
┌──────────────────┐
│ loading = TRUE   │  ← Still says "I'm fetching!"
│ data = [old]     │
│                  │
│ Code just says:  │
│ "Oh, aborted?    │
│  Never mind."    │
│                  │
│ BUT FORGETS TO   │
│ SET loading=FALSE│  ← 🐛 THE BUG!
└──────────────────┘

STEP 3: Nothing triggers a new request
┌──────────────────┐
│ loading = TRUE   │  ← STUCK! System thinks it's still loading
│ data = [old]     │
│                  │
│ UI shows:        │
│ "⟳ Updating..."  │  ← Forever...
└──────────────────┘
```

---

## Issue #2: The "NaNo" Display Bug

### What You See
```
┌─────────────────────────────────┐
│ DIVERGENCE                      │
│ NaNo                            │  ← What does "NaNo" mean??
│ Aligned                         │
└─────────────────────────────────┘
```

### What's Happening

```
THE MATH PROBLEM:

When there's no data:
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│   latestZCcrRcr = undefined   (no data loaded yet)             │
│   latestZRcrOcr = undefined   (no data loaded yet)             │
│                                                                 │
│   divergence = undefined - undefined                            │
│              = NaN  (Not a Number)                              │
│                                                                 │
│   Display shows: "NaN" → formatted as "NaNo"                   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘

THE CODE BUG:
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│   // Current code checks for null:                              │
│   if (value === null) return null;  // ✓ catches null          │
│                                                                 │
│   // But undefined slips through!                               │
│   if (value === null) ...           // ✗ undefined !== null    │
│                                                                 │
│   // undefined - undefined = NaN                                │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### The Simple Fix

```
BEFORE (buggy):                    AFTER (fixed):
─────────────────                  ─────────────────
if (value === null)                if (value == null)
     ↑                                  ↑
     │                                  │
Only catches null                  Catches BOTH null AND undefined
```

---

## Issue #3: The Shared Data Pattern

### The Good Intention (Performance Optimization)

```
BEFORE: 3 separate API calls (wasteful)
─────────────────────────────────────────
┌─────────────┐
│ Chart A     │──────▶ API Call 1 ──▶ Same data
└─────────────┘
┌─────────────┐
│ Chart B     │──────▶ API Call 2 ──▶ Same data  (duplicate!)
└─────────────┘
┌─────────────┐
│ Chart C     │──────▶ API Call 3 ──▶ Same data  (duplicate!)
└─────────────┘


AFTER: 1 shared API call (efficient) ✓
─────────────────────────────────────────
                    ┌─────────────┐
                    │ Chart A     │
                    └─────────────┘
                          ↑
┌──────────┐              │
│ Parent   │──▶ API ──▶ SHARED DATA + STATUS
│ Page     │              │
└──────────┘              ↓
                    ┌─────────────┐
                    │ Chart B     │
                    └─────────────┘
                          │
                          ↓
                    ┌─────────────┐
                    │ Chart C     │
                    └─────────────┘
```

### The Unintended Consequence

```
WHEN PARENT STATUS GETS STUCK:
─────────────────────────────────────────

┌──────────────────────────────────────────────────────────────────┐
│  PARENT PAGE                                                     │
│  ┌────────────────────────────────────┐                         │
│  │ Status: "refreshing" (STUCK!)      │                         │
│  │ Data: []                           │                         │
│  └────────────────────────────────────┘                         │
│           │                                                      │
│           │ passes status to ALL children                        │
│           ▼                                                      │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐              │
│  │ Chart A     │  │ Chart B     │  │ Chart C     │              │
│  │             │  │             │  │             │              │
│  │ ⟳ Updating  │  │ ⟳ Updating  │  │ ⟳ Updating  │              │
│  │             │  │             │  │             │              │
│  └─────────────┘  └─────────────┘  └─────────────┘              │
│                                                                  │
│  ALL CHARTS STUCK because they inherit parent's stuck status!   │
└──────────────────────────────────────────────────────────────────┘

THIS IS NOT A BUG IN THE SHARED PATTERN!
The bug is WHY the parent status gets stuck (Issue #1)
```

---

## Issue #4: The Boot Sequence Can Get Stuck

### Normal Boot (Happy Path) ✓

```
┌─────┐    ┌─────┐    ┌─────┐    ┌─────┐    ┌─────┐
│  1  │───▶│  2  │───▶│  3  │───▶│  4  │───▶│  ✓  │
└─────┘    └─────┘    └─────┘    └─────┘    └─────┘
Firebase   Token      Sub-       Filters    APP
Auth       Sync       scription  Ready      READY!

Time: 0s    0.5s      1s         1.2s       1.5s
```

### Stuck Boot (Bug Path) ✗

```
┌─────┐    ┌─────┐
│  1  │───▶│  2  │───▶ ❌ STUCK!
└─────┘    └─────┘
Firebase   Token
Auth ✓     keeps
           failing

Time: 0s    0.5s ──────────────────────▶ Forever...

                      ┌──────────────────────────────┐
                      │ Token Status: "refreshing"   │
                      │                              │
                      │ Subscription: "I'm waiting   │
                      │ for token to be ready..."    │
                      │                              │
                      │ App Ready: FALSE             │
                      │                              │
                      │ Charts: "Can't load until    │
                      │ app is ready!"               │
                      └──────────────────────────────┘
```

### The Recovery Banner (Partial Fix)

```
After 10 seconds of being stuck, a banner appears:

┌─────────────────────────────────────────────────────────────────┐
│ ⚠️ Loading taking longer than expected (subscription)  [Retry] │
└─────────────────────────────────────────────────────────────────┘

User clicks Retry → Manually breaks the deadlock

BUT: This requires user action. Ideally it should auto-recover.
```

---

## Summary: The 4 Main Problems

```
┌─────────────────────────────────────────────────────────────────────┐
│                        PROBLEM SUMMARY                              │
├──────┬──────────────────────────────┬───────────────┬───────────────┤
│  #   │  PROBLEM                     │  SYMPTOM      │  SEVERITY     │
├──────┼──────────────────────────────┼───────────────┼───────────────┤
│  1   │  Abort doesn't reset         │  "Updating"   │  🔴 CRITICAL  │
│      │  loading status              │  forever      │               │
├──────┼──────────────────────────────┼───────────────┼───────────────┤
│  2   │  Missing null/undefined      │  "NaNo"       │  🟡 MEDIUM    │
│      │  check in math               │  display      │               │
├──────┼──────────────────────────────┼───────────────┼───────────────┤
│  3   │  Token refresh can get       │  Boot stuck   │  🟠 HIGH      │
│      │  stuck in "refreshing"       │  forever      │               │
├──────┼──────────────────────────────┼───────────────┼───────────────┤
│  4   │  No automatic timeout        │  Manual       │  🟡 MEDIUM    │
│      │  recovery for stuck states   │  retry needed │               │
└──────┴──────────────────────────────┴───────────────┴───────────────┘
```

---

## The Fixes Needed

```
┌─────────────────────────────────────────────────────────────────────┐
│                         FIXES OVERVIEW                              │
└─────────────────────────────────────────────────────────────────────┘

FIX #1: Reset loading status on abort
────────────────────────────────────
BEFORE:                          AFTER:
if (aborted) {                   if (aborted) {
  return; // do nothing            loading = false; // reset!
}                                  return;
                                 }


FIX #2: Better null check
────────────────────────────────────
BEFORE:                          AFTER:
if (value === null)              if (value == null)  // catches both!


FIX #3: Add timeout recovery
────────────────────────────────────
NEW CODE:
if (stuck for more than 30 seconds) {
  force status to "error"
  show retry button
}


FIX #4: Auto-retry on boot stuck
────────────────────────────────────
NEW CODE:
if (boot stuck for 10 seconds) {
  automatically retry token + subscription
  (don't wait for user to click)
}
```

---

## Visual Timeline of the Bug

```
TIMELINE OF WHAT HAPPENS:

0.0s  ┃ User loads page
      ┃
0.5s  ┃ Firebase: "User is logged in!" ✓
      ┃
1.0s  ┃ Token sync: "Getting JWT token..."
      ┃
1.5s  ┃ Token sync: "Got it!" ✓
      ┃
2.0s  ┃ Subscription: "Checking if premium..."
      ┃
2.5s  ┃ Subscription: "User is premium!" ✓
      ┃
3.0s  ┃ App Ready: TRUE ✓
      ┃
3.0s  ┃ Charts: "Starting to fetch data..."
      ┃
3.2s  ┃ User changes a filter quickly
      ┃
3.2s  ┃ Old request: ABORTED
      ┃ New request: Started
      ┃
3.3s  ┃ Network hiccup - new request also fails
      ┃
3.3s  ┃ Status: Still "refreshing" (loading=true never reset)
      ┃
3.3s  ┃ ─────────────────────────────────────────────
      ┃   ⟳ Updating...  (STUCK FOREVER)
      ┃ ─────────────────────────────────────────────
      ▼
    ∞ s  User sees spinner forever until page refresh
```

---

## Questions?

If any part is still unclear, let me know and I can explain further!
