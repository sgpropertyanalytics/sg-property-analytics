[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
markers =
    smoke: fast endpoint smoke checks (deselect with '-m "not smoke"')
    integration: tests that require external services or full DB
    regression: snapshot-based regression tests
