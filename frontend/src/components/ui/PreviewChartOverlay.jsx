/**
 * Premium preview overlay has been retired.
 * Keep this wrapper as a passthrough for compatibility.
 */

export function PreviewChartOverlay({ children }) {
  return <>{children}</>;
}

export default PreviewChartOverlay;
