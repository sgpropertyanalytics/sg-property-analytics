/**
 * Premium watermark has been retired.
 * Keep this wrapper as a passthrough for compatibility.
 */
export function ChartWatermark({ children }) {
  return children;
}

export default ChartWatermark;
