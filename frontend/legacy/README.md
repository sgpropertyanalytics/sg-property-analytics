This folder contains legacy static assets that are no longer part of the main React + Vite frontend.

- `dashboard.html` – old standalone dashboard implementation, kept only for historical reference.

The production app is built entirely from the React source in `src/`.


